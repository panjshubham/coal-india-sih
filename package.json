{
  "name": "coalguard-root",
  "version": "1.0.0",
  "description": "Root workspace for CoalGuard",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
